############################################################
# Environment variables for Vite (client-exposed)
#
# How to use .env files:
# 1) Copy this file to one of the following depending on your need:
#    - .env                 (applies to all modes)
#    - .env.local           (local-only, not committed)
#    - .env.development     (only when running in dev mode)
#    - .env.production      (only when building for production)
# 2) Fill in the placeholders below (no quotes needed).
# 3) Ensure your .env* files are ignored by Git (see note below).
#
# Vite note:
# - Only variables prefixed with VITE_ are exposed to your client code
#   via import.meta.env.
# - Do NOT put secrets without the VITE_ prefix if they must remain server-side.
#
# Git ignore tip (add to .gitignore if not present):
#   .env*
############################################################

# Base URLs
VITE_API_JOKES1_BASE_URL=https://icanhazdadjoke.com/
VITE_API_JOKES2_BASE_URL=https://api.chucknorris.io/jokes/random
VITE_API_WEATHER_BASE_URL=https://api.openweathermap.org/data/2.5/weather

# API Keys (replace with your actual key)
VITE_WEATHER_API_KEY=YOUR_OPENWEATHER_API_KEY